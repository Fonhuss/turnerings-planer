import React from "react";
import ReactDOM from "react-dom/client";
import Planlæggeren from "./planlæggeren";

ReactDOM.createRoot(document.getElementById("root")!).render(
  <React.StrictMode>
    <Planlæggeren />
  </React.StrictMode>
);
